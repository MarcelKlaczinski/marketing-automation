# Spec 63.6 — Brief-Approve mit Plan-vs-Immediate Dispatch

_Theme 63 Sub-Spec — trennt Brief-Approve vom direkten Article-Trigger. Default-Pfad geht durch Plan-Workflow (Budget-Gate aktiv). Direct-Generate bleibt als bewusster Override._
_Status: Draft._
_Voraussetzung: 62.8 gemerged (Plan-Workflow steht), Diagnose 2026-05-21 (Workflow-Konflikt verstanden)._

---

## 1. Problem

Aktuell macht `POST /api/projects/:slug/briefs/bulk-approve`:

```
pending → routed (sofort) + article-INSERT + pipeline-enqueue
```

Theme 62 hat einen Plan-Layer eingeführt mit Budget-Gate (90%), Scheduling, llmMode-Snapshot. Aber Brief-Approve bypasst diesen Layer komplett. Marcel kann unbeabsichtigt Geld verbrennen wenn er Briefs außerhalb des Plan-Flows approved.

Konkretes Symptom (heute): 7 missing_hub Briefs approved → 7 article:blog Pipelines gestartet → alle failed via Anthropic-400 → in 'routed' Status festgenagelt. Aus Planner-Sicht "weg". Hätte ohne Error €3-14 Spend verursacht ohne Plan-Visibility.

## 2. Ziel

Zwei explizite Dispatch-Modi am bulk-approve Endpoint:

- **`dispatch: 'plan'`** (DEFAULT): Brief flippt zu `plan_pending`, wartet auf Planner-Pickup, kein Article-Insert, kein Pipeline-Enqueue
- **`dispatch: 'immediate'`** (Override): bestehende Mechanik — sofort Article-Insert + Pipeline-Enqueue

**Marcel's UX:**
- Default-Click in BriefsPage → "In nächsten Plan eingeplant"
- Separater "Jetzt generieren"-Button → bestehender Direct-Flow (1-2x pro Monat)

**In Scope:**
- Backend: dispatch-Param, neuer Brief-Status `plan_pending`, Migration
- Backend: Planner-Loader erweitern um plan_pending zu picken
- Backend: alle bestehenden Caller (Single-Approve, autoApproveGaps, ...) auf explizites dispatch umstellen
- Frontend: zweite Button-Variante "Jetzt generieren" in BriefsPage + BriefDetailPage
- Status-Übergang plan_pending → routed beim Plan-Pickup (Planner-Step)

**Out of Scope:**
- Article-Retry-Pfad ändern (existing /articles/:id/retry bleibt)
- Quarantäne erweitern für gescheiterte article-from-brief Runs (separate Spec falls nötig)
- 7 routed Briefs cleanen (Marcel macht via Anthropic-Fix-Retry separat, β-Pfad)

**Aufwand:** 0.5-1 Tag.

---

## 3. Architektur-Entscheidungen

### 3.1 Brief-Lifecycle erweitert

```
                                    ┌─── (dispatch: immediate) ──┐
                                    │                              │
pending ──(bulk-approve)──┬─────────┘                              ↓
                          │                                     routed ──→ article exists
                          └─── (dispatch: plan, default) ──┐       ↑
                                                            ↓       │
                                                     plan_pending ──┘
                                                            │
                                                            ↓ (planner-step picks up)
                                                       (item created, brief stays plan_pending until plan-execute marks routed)
```

**Wichtig:** `plan_pending → routed` Transition passiert in **2 Stufen**:
1. **Plan-Generation:** SelectFloorItemsStep zieht plan_pending Brief, erstellt planned_item, Brief **bleibt** plan_pending (Marcel kann Plan noch verwerfen + Brief landet zurück im Pool)
2. **Plan-Execute (62.8):** wenn planned_item zu generating wechselt, Brief flippt zu routed

Lebt-noch-im-Pool-Property bewahrt: Briefs bleiben im plan_pending bis tatsächliche Generation startet, kein "stranded" State bei Plan-Cancel.

### 3.2 Status-Enum-Erweiterung

`topic_briefs.approval_status` aktueller Wert-Set (vermutet aus Discovery):
- `pending`
- `routed`
- `dismissed` (vermutet — Phase 0 verifiziert)
- `skipped` (vermutet — Phase 0 verifiziert)

Neu:
- `plan_pending` — Marcel approved, wartet auf Plan-Pickup

Migration via CHECK-Widening (Memory D134: eigene Migration nötig, nicht inline mit anderen ALTER TABLE).

### 3.3 dispatch-Param

`bulkApproveSchema` (Zod):

```ts
const bulkApproveSchema = z.object({
  briefIds: z.array(z.string().uuid()).min(1),
  dispatch: z.enum(['plan', 'immediate']).default('plan'),  // NEU
});
```

Default = 'plan' — sicherer Default schützt vor versehentlichen Direct-Spend.

### 3.4 Brief-Service Branch

`apps/api/src/lib/brief-service.ts`:

```ts
async function approveBriefs({ briefIds, dispatch, ... }) {
  if (dispatch === 'plan') {
    // Nur Status-Flip
    return await markBriefsPlanPending(briefIds);
  }
  
  if (dispatch === 'immediate') {
    // Bestehende Mechanik: pending → routed + article-INSERT + pipeline-enqueue
    return await approveBriefAndEnqueue(briefIds);  // existing logic
  }
}
```

**Decision-table-Pattern**, kein conditional embedded — leicht zu testen.

### 3.5 Planner-Loader erweitern

`packages/planner/src/load-topic-briefs.ts`:

```ts
// VORHER:
.where(eq(topicBriefs.approvalStatus, 'pending'))

// NACHHER:
.where(inArray(topicBriefs.approvalStatus, ['pending', 'plan_pending']))
```

**Begründung pending zu behalten:** Trend-Synthesizer-Briefs sind direkt nach Erzeugung im `pending` und können vom Planner gepicked werden (Cold-Start-fähig). plan_pending ist für Marcel-explicitly-approved Briefs.

**Eventuelles Refactor später:** mit 63.4 (Trend-Synthesizer ki_wissen support) kommt vielleicht auch ein neuer Workflow wo Trend-Briefs eine "approval"-Stufe brauchen. Aktuell noch nicht.

### 3.6 Status-Transition beim Plan-Execute

`packages/pipelines/src/planning/steps/execute-item.ts` (oder wo planned_item zu 'generating' flippt — Phase 0 findet exakte Stelle):

```ts
async function markItemGenerating(item, ctx) {
  await markPlannedItemInProgress(item.id);
  
  // NEU: Brief-Status-Update wenn Item auf Brief basiert
  if (item.sourceKind === 'topic_brief' && item.sourceId) {
    await markBriefRoutedFromPlan(item.sourceId, item.id);
  }
}
```

`markBriefRoutedFromPlan` analog `markBriefRouted` aber:
- Filtert WHERE `approval_status = 'plan_pending'` (CAS)
- Setzt routed_article_id NICHT (kein Article existiert noch — wird vom article:blog Pipeline-Run später erzeugt)
- Setzt `routed_via_plan_item_id` als neue Column (oder reused: `routed_from`)

**Wichtig:** plan_pending Brief hat KEINE routed_article_id. Wenn Marcel im UI eines anschaut: "Wartet auf Plan-Pickup" Status, kein Article-Link.

### 3.7 Bestehende Caller migrieren

Phase 0 Discovery zeigt alle Stellen. Vermutete Liste:

| Caller | Aktueller dispatch | Soll-dispatch |
|---|---|---|
| BriefsPage Bulk-Approve | (kein Param) | 'plan' (default) |
| BriefDetailPage Single-Approve | (kein Param) | 'plan' (default) |
| `/suggest?autoApproveGaps=true` (Spec 49c) | (kein Param) | 'plan' (Cron-driven, kein Sofort-Spend) |
| Trend-Brief auto-approval (falls existiert) | (kein Param) | wahrscheinlich 'plan' |

**Strategie:** alle bestehenden Caller passen kein dispatch — kriegen Default 'plan'. Wer Direct-Generate will, muss explizit `dispatch: 'immediate'` setzen.

**Audit-Pfad:** `grep "dispatch.*immediate"` zeigt alle Direct-Generate-Stellen. Aktuell wahrscheinlich nur 1: ein neuer "Jetzt generieren"-Button in UI.

---

## 4. Datenbank-Migration

### 4.1 Migration `008X_brief_plan_pending_status.sql`

(Phase 0 verifiziert nächste freie Nummer)

```sql
-- 1) Widen CHECK constraint to allow 'plan_pending'
ALTER TABLE topic_briefs
  DROP CONSTRAINT IF EXISTS topic_briefs_approval_status_check;

ALTER TABLE topic_briefs
  ADD CONSTRAINT topic_briefs_approval_status_check
  CHECK (approval_status IN ('pending', 'plan_pending', 'routed', 'dismissed', 'skipped'));
  -- Phase 0 Discovery: exakte Liste verifizieren!
```

**Lesson Memory D134:** eigene Migration für CHECK-Widening, nicht inline mit anderen ALTER TABLE.

### 4.2 Optionale Column `routed_via_plan_item_id`

Wenn Phase 0 zeigt dass Brief→Article-Verbindung über `routed_article_id` läuft, ist das ausreichend. Falls Marcel auch wissen will "welches planned_item triggered diese routing":

```sql
ALTER TABLE topic_briefs
  ADD COLUMN routed_via_plan_item_id uuid REFERENCES planned_items(id);
```

**Optional** — wenn Spec-Implementor sieht dass nicht nötig, weglassen.

---

## 5. Backend — Implementation

### 5.1 Schema-Update

`packages/db/src/schema/topic-briefs.ts`:

```ts
export const topicBriefs = pgTable("topic_briefs", {
  // ...
  approvalStatus: text("approval_status").notNull().default("pending").$type<
    "pending" | "plan_pending" | "routed" | "dismissed" | "skipped"
  >(),
  routedViaPlanItemId: uuid("routed_via_plan_item_id").references(() => plannedItems.id),  // optional
  // ...
});
```

### 5.2 Brief-Service-Branch

`apps/api/src/lib/brief-service.ts`:

```ts
export type Dispatch = 'plan' | 'immediate';

export async function approveBriefs({
  projectId,
  briefIds,
  dispatch = 'plan',
}: {
  projectId: string;
  briefIds: string[];
  dispatch?: Dispatch;
}) {
  // CAS-WHERE pending only (idempotent)
  const briefs = await db.query.topicBriefs.findMany({
    where: and(
      eq(topicBriefs.projectId, projectId),
      inArray(topicBriefs.id, briefIds),
      eq(topicBriefs.approvalStatus, 'pending'),
    ),
  });
  
  if (briefs.length === 0) {
    return { processed: [], skipped: briefIds };
  }
  
  if (dispatch === 'plan') {
    return await markBriefsPlanPending(briefs);
  } else {
    return await approveBriefsAndEnqueue(briefs);  // existing
  }
}

async function markBriefsPlanPending(briefs: TopicBrief[]) {
  const ids = briefs.map(b => b.id);
  await db
    .update(topicBriefs)
    .set({
      approvalStatus: 'plan_pending',
      approvedAt: new Date(),
    })
    .where(inArray(topicBriefs.id, ids));
  
  return {
    processed: briefs.map(b => ({ id: b.id, kind: 'plan_pending' as const })),
    skipped: [],
  };
}
```

### 5.3 Endpoint-Update

`apps/api/src/routes/projects/briefs.ts`:

```ts
const bulkApproveSchema = z.object({
  briefIds: z.array(z.string().uuid()).min(1).max(100),
  dispatch: z.enum(['plan', 'immediate']).default('plan'),
});

briefs.post('/bulk-approve', zValidator('json', bulkApproveSchema), async (c) => {
  const { briefIds, dispatch } = c.req.valid('json');
  const projectId = ...; // from auth
  
  const result = await approveBriefs({ projectId, briefIds, dispatch });
  
  return c.json(result);
});
```

### 5.4 Planner-Loader

`packages/planner/src/load-topic-briefs.ts`:

```ts
export async function loadPendingTopicBriefs(projectId: string) {
  return await db.query.topicBriefs.findMany({
    where: and(
      eq(topicBriefs.projectId, projectId),
      inArray(topicBriefs.approvalStatus, ['pending', 'plan_pending']),
    ),
    orderBy: [
      sql`CASE WHEN approval_status = 'plan_pending' THEN 0 ELSE 1 END`,  // prefer plan_pending
      desc(topicBriefs.createdAt),
    ],
  });
}
```

**ORDER BY:** plan_pending Briefs werden bevorzugt — Marcel hat sie explizit approved, also höhere Priorität.

### 5.5 Plan-Execute-Hook

`packages/pipelines/src/execution/...` — Phase 0 findet exakte Stelle wo planned_item zu 'generating' flippt:

```ts
async function transitionItemToGenerating(item, ctx) {
  await markPlannedItemInProgress(item.id);
  
  if (item.sourceKind === 'topic_brief' && item.sourceId) {
    await db.update(topicBriefs)
      .set({ 
        approvalStatus: 'routed',
        routedViaPlanItemId: item.id,
      })
      .where(and(
        eq(topicBriefs.id, item.sourceId),
        eq(topicBriefs.approvalStatus, 'plan_pending'),  // CAS
      ));
  }
}
```

---

## 6. Frontend

### 6.1 BriefsPage Bulk-Approve

Aktuell: Toggle-Selection + "Approve"-Button → ein Endpoint-Call.

Neu: zwei Buttons in BulkApproveModal:

```vue
<template>
  <q-card>
    <q-card-section>
      <div>{{ selectedCount }} Briefs ausgewählt</div>
    </q-card-section>
    
    <q-card-actions>
      <q-btn
        flat
        :label="$t('briefs.bulkApprove.plan')"
        @click="approve('plan')"
        color="primary"
      />
      <q-btn
        flat
        :label="$t('briefs.bulkApprove.immediate')"
        @click="approve('immediate')"
        color="warning"
      />
    </q-card-actions>
  </q-card>
</template>

<script>
export default {
  methods: {
    approve(dispatch) {
      this.$emit('approve', { briefIds: this.selectedIds, dispatch });
    },
  },
};
</script>
```

**i18n:**

```ts
// DE
briefs: {
  bulkApprove: {
    plan: 'In nächsten Plan einplanen',
    planConfirm: 'Briefs landen im Plan-Pool. Generation startet wenn du den nächsten Wochenplan approvest.',
    immediate: 'Jetzt sofort generieren',
    immediateConfirm: 'Artikel werden sofort erzeugt — Budget-Gate wird umgangen. Nur für Edge-Cases empfohlen.',
  },
}

// EN
briefs: {
  bulkApprove: {
    plan: 'Schedule for next plan',
    planConfirm: 'Briefs will land in the plan pool. Generation starts when you approve the next weekly plan.',
    immediate: 'Generate immediately',
    immediateConfirm: 'Articles will be generated immediately — budget gate is bypassed. For edge cases only.',
  },
}
```

### 6.2 BriefDetailPage Single-Approve

Gleiche zwei-Button-Variante. Beim "Jetzt sofort generieren"-Klick: Confirm-Dialog wegen Budget-Risiko.

### 6.3 BriefsPage Status-Indikator

Pending-Section zeigt jetzt plan_pending Briefs separat (oder mit Badge):

```
Pending Section:
  ⚪ Pending (X)
  🟢 In Plan-Pool (Y) ← neue Kategorie für plan_pending
```

Oder via Chip-Reihe (Filter-Pattern aus BriefsPage-Update):
- `?readiness=plan_ready` (NEU) — plan_pending Briefs
- `?readiness=enriched` (existing) — Briefs mit primary_keyword aber pending
- etc.

**Implementor-Entscheidung:** ob neue Chip oder separate Section. Beide funktionieren.

### 6.4 Success-Toast

```
Approve via 'plan':       "5 Briefs für nächsten Plan vorgemerkt"
Approve via 'immediate':  "5 Briefs werden sofort generiert"
```

---

## 7. Tests

### 7.1 Backend

`apps/api/test/`:
- `bulk-approve-plan-dispatch.test.ts`:
  - dispatch='plan' (default): Briefs flippen zu plan_pending, kein Article-Insert
  - dispatch='immediate': bestehende Mechanik
  - Schema-Validation: dispatch muss 'plan' | 'immediate' sein
  - Idempotenz: re-approve plan_pending Briefs → skipped
- `loadTopicBriefs-plan-pending.test.ts`:
  - Loader picked sowohl pending als auch plan_pending
  - ORDER BY plan_pending zuerst
- `plan-execute-routed-brief.test.ts`:
  - Wenn planned_item zu generating flippt, source-Brief flippt zu routed
  - CAS: nur plan_pending → routed Übergang, andere ignoriert

### 7.2 Frontend (manuell)

E2E-Smoke:
1. BriefsPage öffnen → 2 Briefs selektieren → "In Plan einplanen" → Toast "vorgemerkt", Briefs verschwinden aus pending, erscheinen in plan_pending Section
2. Plan-Generation triggern (Cron oder manuell) → die 2 plan_pending Briefs landen als planned_items
3. Plan approven (62.8) → planned_items flippen zu generating → Briefs flippen zu routed
4. BriefsPage zeigt Briefs jetzt im routed Status mit Article-Link

5. Bei einem dritten Brief: "Jetzt sofort generieren" → Confirm-Dialog → ok → Toast "wird sofort generiert", Brief flippt direkt zu routed mit Article-Link, Pipeline läuft

### 7.3 PR-Merge-Gate

- Tests grün
- Workspace-typecheck 0 errors
- Migration sauber durch
- Manuelle E2E 1-5 dokumentiert
- Alle bestehenden Caller des bulk-approve auf explizites dispatch geprüft

---

## 8. PR-Merge-Reihenfolge

**Phase 0: Discovery (~15-30 Min)**
1. Exakte topic_briefs.approval_status Werte verifizieren (CHECK constraint lesen)
2. Bestehende Caller des bulk-approve finden
3. Wo flippt planned_item zu 'generating' im 62.8-Code
4. nächste freie Migration-Nummer

**Phase 1: Schema (Day 1 Morning)**
5. Migration für CHECK-Widening
6. Drizzle schema update + Zod types
7. Optional: routed_via_plan_item_id Column

**Phase 2: Backend (Day 1)**
8. brief-service.ts: approveBriefs Branch
9. Endpoint: bulkApproveSchema mit dispatch
10. load-topic-briefs.ts: plan_pending zu inArray
11. plan-execute Hook: plan_pending → routed Transition

**Phase 3: Caller-Migration (Day 1)**
12. Single-Approve (BriefDetailPage Endpoint?): default plan
13. autoApproveGaps (Spec 49c): default plan
14. Andere Caller auf default plan

**Phase 4: Frontend (Day 1 afternoon)**
15. BulkApproveModal zwei Buttons
16. BriefDetailPage zwei Buttons
17. i18n DE+EN
18. Success-Toasts
19. plan_pending Section/Chip in BriefsPage

**Phase 5: Tests + Polish (Day 1 end)**
20. Backend-Tests
21. Manuelle E2E
22. CLAUDE.md updates
23. PR-Merge

---

## 9. Risiken

| Risiko | Mitigation |
|---|---|
| Bestehende Caller setzen kein dispatch → bekommen Default 'plan' → User wundert sich warum direkter Article-Trigger nicht mehr passiert | Phase 3 Discovery findet alle Caller. Vor Merge alle expliziert auf gewünschten dispatch setzen. |
| Plan-Generation pickt plan_pending Brief, aber Marcel verwirft den Plan → Brief im Limbo? | plan_pending Brief bleibt im Pool bis tatsächliche Generation startet. Marcel kann plan-cancel → Briefs sind wieder im Pool. |
| Brief-Status-Transition race mit Plan-Cancel | plan_pending → routed nur via CAS-Update (WHERE status='plan_pending'). Concurrent Cancel kann Status nicht in Vergangenheit ändern. |
| Marcel klickt "Jetzt sofort generieren" ohne Confirm zu lesen → Budget verbrennt | Confirm-Dialog mit "Budget-Gate wird umgangen" Wording. Override hinter zwei Klicks. |
| autoApproveGaps Cron (Spec 49c) generiert weiter sofort, weil noch kein dispatch=plan gesetzt | Phase 3 expliziter Code-Change: autoApproveGaps callt mit dispatch='plan'. Plus Test. |
| Plan-Execute Hook race mit anderen Brief-Updates | CAS-Pattern: Update mit WHERE approval_status='plan_pending'. Lost-update wird im DB-Layer abgefangen. |
| 7 routed Briefs aus dem heutigen Bulk-Approve sind jetzt im Limbo | Marcel cleant via Article-Retry (β-Pfad) wenn Anthropic-Fix kommt. Außerhalb 63.6-Scope. |

---

## 10. Akzeptanzkriterien

63.6 ist DONE wenn:

1. ✅ Migration läuft sauber durch (CHECK widening)
2. ✅ topic_briefs.approval_status erlaubt 'plan_pending'
3. ✅ bulkApproveSchema akzeptiert optional dispatch='plan'|'immediate', default 'plan'
4. ✅ approveBriefs mit dispatch='plan': pending → plan_pending, kein Article-Insert
5. ✅ approveBriefs mit dispatch='immediate': bestehende Mechanik
6. ✅ Planner-Loader picked pending UND plan_pending Briefs
7. ✅ plan_pending werden bevorzugt in der ORDER BY
8. ✅ Beim Plan-Execute (Item generating): plan_pending Brief flippt zu routed
9. ✅ BriefsPage zeigt zwei Buttons mit klarer DE+EN i18n
10. ✅ BriefsPage zeigt plan_pending Briefs sichtbar (eigene Section oder Chip-Filter)
11. ✅ Alle bestehenden Caller explizit auf default 'plan' migriert
12. ✅ Backend-Tests grün, Workspace-typecheck 0 errors
13. ✅ Manuelle E2E 1-5 dokumentiert

---

## 11. Anschluss

Nach 63.6:
- **63.4** Trend-Synthesizer ki_wissen support (kann sequentiell danach)
- **63.5** Floor topic-diversity via embeddings
- 7 routed Briefs aufräumen sobald Anthropic-Fix da ist

**Bewusst NICHT in 63.6:**
- Brief-Status zwischen plan_pending und routed (z.B. plan_active während Plan generiert wird)
- Cancel-Plan-Side-Effect: plan_pending Brief zurück auf pending bei Plan-Cancel? Heuristik unklar — defer
- Article-Retry-Pfad für routed Briefs deren Generation failed
- Approval-Reason-Field (warum hat Marcel approved)
