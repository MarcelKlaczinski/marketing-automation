# Discovery: Social Generation — Ist-Stand für Spec 60.6

**Purpose:** Understand exactly how social posts are triggered, how templates are
selected today, what eligibility logic exists, and where the extension points are
for LLM-based template suggestion + UI template picker.

**Output:** `specs/_drafts/60-6-social-generation-discovery.md`

**Runtime:** ~15 min

---

## Q1: Trigger — wie wird heute ein Social Post gestartet?

Finde alle Einstiegspunkte für Social-Post-Generierung:

```bash
grep -rn "social\|SocialPost\|social_post\|social-post" \
  apps/api/src/routes/ --include="*.ts" | grep -v node_modules | grep -v test
```

Für jeden gefundenen Endpoint:
1. HTTP-Methode + Route-Pfad
2. Was sind die Pflicht-Inputs? (articleId, templateKey, locale, theme, …)
3. Wird `templateKey` vom Client übergeben oder serverseitig bestimmt?
4. Paste die Handler-Signatur (5–10 Zeilen)

---

## Q2: Template-Selektion heute

```bash
grep -rn "templateKey\|template_key\|selectTemplate\|chooseTemplate\|eligib" \
  packages/pipelines/src/ packages/social/src/ --include="*.ts" | \
  grep -v node_modules | grep -v test | grep -v fixtures
```

1. Gibt es eine Funktion die automatisch ein Template für einen Artikel wählt?
   Wenn ja: wo liegt sie, wie funktioniert sie?
2. Gibt es eine Eligibility-Check-Funktion? Wo liegt sie, was prüft sie?
3. Wird `templateKey` heute immer manuell vom Client gesetzt oder gibt es
   irgendwo eine automatische Wahl?

---

## Q3: Eligibility-System — vollständige Analyse

Finde `eligibility` in allen Template-Definitions:

```bash
grep -rn "eligibility\|minToolCount\|requiredFields" \
  packages/social/src/definitions/ --include="*.ts"
```

1. Liste alle Templates mit ihren Eligibility-Regeln
2. Wo wird die Eligibility zur Laufzeit geprüft? (welche Datei, welche Funktion)
3. Was passiert wenn ein Template nicht eligible ist — wird es gefiltert, geloggt,
   dem User gezeigt?
4. Gibt es bereits eine Funktion `getEligibleTemplates(article)` oder ähnliches?

---

## Q4: UI — Article-Detail Social-Tab

Finde die Vue-Komponente für den Social-Tab:

```bash
find apps/frontend/src -name "*.vue" | xargs grep -l "social\|Social" | \
  grep -i "article\|tab" | head -10
```

Für den gefundenen Social-Tab:
1. Zeigt er eine Template-Auswahl an?
2. Wie wird `templateKey` heute an die API übermittelt?
3. Gibt es einen "Empfohlenes Template"-Bereich?
4. Paste die relevanten Template-Teile (template + API-Call, ~20–30 Zeilen)

---

## Q5: LLM-Template-Suggestion — existiert sie?

```bash
grep -rn "suggest\|recommend\|autoSelect\|auto_select\|templateSuggest" \
  packages/ apps/ --include="*.ts" | grep -v node_modules | grep -v test
```

1. Gibt es bereits irgendeine LLM-basierte oder regelbasierte Template-Suggestion?
2. Wenn nicht: welche Article-Felder wären als Input sinnvoll?
   (intentType, clusterRole, toolCount, category, …)
   Paste die relevanten Felder aus dem `articles`-Schema.

---

## Q6: Social in Article-Pipeline — gibt es einen Hook?

```bash
grep -rn "social\|SocialPipeline\|social_image\|socialImage" \
  packages/pipelines/src/article/ --include="*.ts" | grep -v test
```

1. Gibt es einen optionalen Social-Step in der BlogPipeline?
2. Gibt es ein `autoSocial` Flag auf Projekt- oder Brief-Ebene?
3. Wenn nicht: wo in der 13-Step-Pipeline würde ein optionaler
   `SocialGenerationStep` am sinnvollsten eingeklinkt? (nach welchem Step,
   warum)

---

## Q7: Social-Post DB-Schema

```bash
grep -rn "social_posts\|socialPosts" \
  packages/db/src/schema/ --include="*.ts" | head -5
```

Paste das vollständige Schema der `social_posts`-Tabelle (Spalten + Typen).
Welche Spalten würden für Template-Suggestion-Tracking neu benötigt?
(z.B. `suggested_template`, `suggestion_reason`, `user_override`)
